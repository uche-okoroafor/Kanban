import { Box, Typography } from '@material-ui/core';
import Board from '../../components/Kanban/Board';

export default function Profile(): JSX.Element {
  return (
    <Box m={2}>
      <Typography> this is profile</Typography>
      {/* <Board /> */}
    </Box>
  );
}
